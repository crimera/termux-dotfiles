#!/data/data/com.termux/files/usr/bin/bash

path=$3
filename=$(basename "$path")

termux-notification --action "termux-open $path" -c "saved to: $path" -t "$filename Download finished" --icon download
